# Production deployment configuration for Render.com
# This file tells the deployment platform how to run your application

# ULTRA-OPTIMIZED configuration for 1000+ concurrent users
web: gunicorn --config gunicorn_render_optimized.py application:application

# Alternative configurations:
# web: gunicorn --config gunicorn_render_config.py application:application
# web: gunicorn --workers 4 --worker-class gevent --worker-connections 1000 --bind 0.0.0.0:$PORT application:application
# web: gunicorn --config gunicorn_cloud_config.py application:application
# web: gunicorn --config gunicorn_unix_config.py application:application
